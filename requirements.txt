# Core AI & APIs
google-genai>=1.0.0
google-api-python-client>=2.136.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# Trends
pytrends>=4.9.2

# Video & Media
# NOTE: the renderer uses the MoviePy 2.x API (with_position, resized, vfx.*),
# so 2.x is REQUIRED — a 1.x install would break imports.
moviepy>=2.1.1,<3
imageio>=2.34.0
imageio-ffmpeg>=0.5.1

# Text-to-Speech
# edge-tts = free, no-key neural voices (Hindi/Indian-English) with word-level
# timing for perfectly synced captions. gTTS kept only as an offline-ish fallback.
edge-tts>=7.0.0
gTTS>=2.5.1

# Image Processing
Pillow>=10.3.0
numpy>=1.26.4

# Database
sqlalchemy>=2.0.30
alembic>=1.13.0
# Postgres driver — only needed when DATABASE_URL points at Postgres/Supabase
# (local SQLite needs no driver). psycopg2 honours ?sslmode=require.
psycopg2-binary>=2.9.9

# Validation & Config
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# HTTP & Async
requests>=2.32.2
httpx>=0.27.0
tenacity>=8.3.0

# Dashboard
streamlit>=1.35.0
plotly>=5.22.0
pandas>=2.2.2

# Utilities
aiofiles>=23.2.1
python-slugify>=8.0.4
colorlog>=6.8.2
tqdm>=4.66.4

# Local scheduler (optional — run_scheduler.py falls back to a simple loop)
apscheduler>=3.10.4
